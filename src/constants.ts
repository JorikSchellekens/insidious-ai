import {PluginState} from "./types";

export const DEFAULT_PLUGIN_STATE : PluginState = { openaiKey: "", pluginActive: true, promptSelected: 1, paragraphLimit: 3, id: 1 };
export const DB_VERSION = 1;
